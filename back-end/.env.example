PORT=5000
NODE_ENV=development

# Pinata (required for IPFS uploads)
PINATA_JWT=
# Optional: restrict CORS (comma-separated). Omit or use * for permissive dev.
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: if set, POST /api/files/upload and /api/medical-records/upload require
# header X-API-Key: <value> or Authorization: Bearer <value>
# API_KEY=
